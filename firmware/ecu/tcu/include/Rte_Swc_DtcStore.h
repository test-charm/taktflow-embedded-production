/**
 * @file    Rte_Swc_DtcStore.h
 * @brief   Typed RTE wrappers for Swc_DtcStore (TCU)
 *
 * GENERATED BY arxmlgen v1.0.0 -- DO NOT EDIT
 * Source: TaktflowSystem.arxml
 * Config: project.yaml
 *
 * @standard AUTOSAR, ISO 26262 Part 6
 */
#ifndef RTE_SWC_DTCSTORE_H
#define RTE_SWC_DTCSTORE_H

#include "Rte.h"
#include "Tcu_Cfg.h"

/* ====================================================================
 * Typed RTE Wrappers for Swc_DtcStore
 *
 * Usage: #include "Rte_Swc_DtcStore.h" in Swc_DtcStore source files.
 * Provides type-safe inline read/write instead of raw Rte_Read/Rte_Write.
 * ==================================================================== */

/**
 * @brief  Read DTC_Broadcast_ECU_Source (uint8_t)
 * @param  data  Pointer to receive the signal value
 * @return Rte_StatusType (RTE_E_OK on success)
 */
static inline Rte_StatusType Rte_Read_DTC_Broadcast_ECU_Source(uint8_t *data)
{
    uint32_t tmp;
    Rte_StatusType status = Rte_Read(TCU_SIG_DTC_BROADCAST_ECU_SOURCE, &tmp);
    if (status == RTE_E_OK)
    {
        *data = (uint8_t)tmp;
    }
    return status;
}

/**
 * @brief  Read DTC_Broadcast_FreezeFrame0 (uint8_t)
 * @param  data  Pointer to receive the signal value
 * @return Rte_StatusType (RTE_E_OK on success)
 */
static inline Rte_StatusType Rte_Read_DTC_Broadcast_FreezeFrame0(uint8_t *data)
{
    uint32_t tmp;
    Rte_StatusType status = Rte_Read(TCU_SIG_DTC_BROADCAST_FREEZE_FRAME_0, &tmp);
    if (status == RTE_E_OK)
    {
        *data = (uint8_t)tmp;
    }
    return status;
}

/**
 * @brief  Read DTC_Broadcast_FreezeFrame1 (uint8_t)
 * @param  data  Pointer to receive the signal value
 * @return Rte_StatusType (RTE_E_OK on success)
 */
static inline Rte_StatusType Rte_Read_DTC_Broadcast_FreezeFrame1(uint8_t *data)
{
    uint32_t tmp;
    Rte_StatusType status = Rte_Read(TCU_SIG_DTC_BROADCAST_FREEZE_FRAME_1, &tmp);
    if (status == RTE_E_OK)
    {
        *data = (uint8_t)tmp;
    }
    return status;
}

/**
 * @brief  Read DTC_Broadcast_Number (uint16_t)
 * @param  data  Pointer to receive the signal value
 * @return Rte_StatusType (RTE_E_OK on success)
 */
static inline Rte_StatusType Rte_Read_DTC_Broadcast_Number(uint16_t *data)
{
    uint32_t tmp;
    Rte_StatusType status = Rte_Read(TCU_SIG_DTC_BROADCAST_NUMBER, &tmp);
    if (status == RTE_E_OK)
    {
        *data = (uint16_t)tmp;
    }
    return status;
}

/**
 * @brief  Read DTC_Broadcast_OccurrenceCount (uint8_t)
 * @param  data  Pointer to receive the signal value
 * @return Rte_StatusType (RTE_E_OK on success)
 */
static inline Rte_StatusType Rte_Read_DTC_Broadcast_OccurrenceCount(uint8_t *data)
{
    uint32_t tmp;
    Rte_StatusType status = Rte_Read(TCU_SIG_DTC_BROADCAST_OCCURRENCE_COUNT, &tmp);
    if (status == RTE_E_OK)
    {
        *data = (uint8_t)tmp;
    }
    return status;
}

/**
 * @brief  Read DTC_Broadcast_Status (uint8_t)
 * @param  data  Pointer to receive the signal value
 * @return Rte_StatusType (RTE_E_OK on success)
 */
static inline Rte_StatusType Rte_Read_DTC_Broadcast_Status(uint8_t *data)
{
    uint32_t tmp;
    Rte_StatusType status = Rte_Read(TCU_SIG_DTC_BROADCAST_STATUS, &tmp);
    if (status == RTE_E_OK)
    {
        *data = (uint8_t)tmp;
    }
    return status;
}

#endif /* RTE_SWC_DTCSTORE_H */
