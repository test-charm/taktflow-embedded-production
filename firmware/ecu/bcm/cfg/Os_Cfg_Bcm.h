/**
 * @file    Os_Cfg_Bcm.h
 * @brief   OSEK kernel configuration for BCM — task/schedule-table/stack tables (S-OS-11)
 *
 * GENERATED BY arxmlgen v1.0.0 -- DO NOT EDIT
 * Source: TaktflowSystem.arxml
 * Config: project.yaml
 *
 * @standard AUTOSAR, ISO 26262 Part 6
 */
#ifndef OS_CFG_BCM_H
#define OS_CFG_BCM_H

#include "Os.h"
#include "Os_Cfg_Types.h"

/* ==================================================================
 * Task IDs — one basic task per period group + mandatory idle task
 * (kernel convention, FMEA OS-C-03)
 * ================================================================== */

#define OS_TASK_BCM_1MS               ((TaskType)0u)
#define OS_TASK_BCM_10MS              ((TaskType)1u)
#define OS_TASK_BCM_100MS             ((TaskType)2u)
#define OS_TASK_BCM_IDLE              ((TaskType)3u)
#define OS_TASK_COUNT_BCM             ((uint8)4u)

/* ==================================================================
 * Task priorities — shorter period = higher priority; numeric 0 is the
 * HIGHEST priority (Os_Scheduler.c os_select_next_ready_task scans
 * priority 0 upward). Idle sits at OS_MAX_PRIORITIES-1.
 * ================================================================== */

#define OS_TASK_PRIO_BCM_1MS              ((uint8)0u)
#define OS_TASK_PRIO_BCM_10MS             ((uint8)1u)
#define OS_TASK_PRIO_BCM_100MS            ((uint8)2u)

/* ==================================================================
 * Schedule table IDs — one repeating table per period group
 * ================================================================== */

#define OS_TABLE_BCM_1MS              ((ScheduleTableType)0u)
#define OS_TABLE_BCM_10MS             ((ScheduleTableType)1u)
#define OS_TABLE_BCM_100MS            ((ScheduleTableType)2u)
#define OS_SCHED_TABLE_COUNT_BCM  ((uint8)3u)

/* ==================================================================
 * System counter — 1 tick per legacy RTE dispatch tick.
 * The kernel derives its counter base (AlarmBaseType) from alarm table
 * element 0 when alarms are configured; this configuration uses no
 * alarms, so the kernel default applies. The values below document that
 * base for integrators (must match Os_Core.c defaults).
 * ================================================================== */

#define BCM_OS_COUNTER_MAXALLOWEDVALUE  0xFFFFFFFFu
#define BCM_OS_COUNTER_TICKSPERBASE     1u
#define BCM_OS_COUNTER_MINCYCLE         1u

/* ==================================================================
 * Generated task bodies (Rte_TaskBodies_Bcm.c)
 * ================================================================== */

void Os_Task_Bcm_1ms(void);
void Os_Task_Bcm_10ms(void);
void Os_Task_Bcm_100ms(void);
void Os_Task_Bcm_Idle(void);

/* Aggregate kernel configuration — pass to Os_Configure() before StartOS */
extern const Os_ConfigType bcm_os_config;

/**
 * @brief   Start all period-group schedule tables (absolute start 0).
 * @return  E_OK, or the last non-OK status from StartScheduleTableAbs.
 * @note    Called once by the generated idle task (the autostarted task
 *          context) after StartOS: StartScheduleTableAbs is rejected
 *          outside TASK/ISR2 call level by the SC3 service protection.
 */
StatusType Bcm_Os_StartScheduleTables(void);

#endif /* OS_CFG_BCM_H */
